# Server
PORT=8080
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# OTP policy
OTP_LENGTH=6
OTP_TTL_SECONDS=600
OTP_RATE_LIMIT_WINDOW_SECONDS=900
OTP_MAX_PER_WINDOW=5
OTP_MIN_RESEND_INTERVAL_SECONDS=60

# Email provider (choose one path)
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_app_password
# EMAIL_FROM="Empresario Registration <your_email@example.com>"

# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your_key
# EMAIL_FROM="Empresario Registration <noreply@yourdomain.com>"

# Google Service Account credentials
# Provide either the base64 of the JSON or a path to the json
GOOGLE_CREDENTIALS_JSON_BASE64=
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Google Sheets
SPREADSHEET_ID=
SHEET_NAME=Submissions